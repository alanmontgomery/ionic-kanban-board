import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.kanban.board',
  appName: 'ionic-react-kanban-board',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
